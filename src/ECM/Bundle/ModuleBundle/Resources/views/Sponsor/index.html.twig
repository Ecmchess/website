{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sponsor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Urlimage</th>
                <th>Lien</th>
                <th>Ordre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_sponsor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titre }}</td>
                <td>{{ entity.urlImage }}</td>
                <td>{{ entity.lien }}</td>
                <td>{{ entity.ordre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_sponsor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_sponsor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_sponsor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
