{% extends 'ECMAdminBundle::layout.html.twig' %}

{% block titre %}
    <h1>Éditer un sponsor</h1>
{% endblock %}

{% block body -%}

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_sponsor') }}">
            Retourner à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
