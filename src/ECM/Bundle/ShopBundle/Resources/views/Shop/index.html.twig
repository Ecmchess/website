{% extends 'ECMHomeBundle::layout.html.twig' %}

{% block body -%}
    <h1>Liste Produits</h1>

    <table class="table table-striped table-hover ">

        <tbody>
        {% for entity in entities %}
            <tr>
                <td><img src="/ecmchess/web/images/uploads/{{ entity.image.webPath }}" class="product-preview"></td>
                <td><a href="{{ path('ecm_shop_ProductById', { 'id': entity.id }) }}">{{ entity.nom }}</a></td>

                <td>{{ entity.prix }} €</td>


            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
